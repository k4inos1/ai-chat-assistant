[project]
name = "ai-chat-backend"
version = "0.1.0"
description = "AI Chat Assistant - FastAPI backend with OpenAI integration"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "openai>=1.30.0",
    "pydantic>=2.0.0"
]
